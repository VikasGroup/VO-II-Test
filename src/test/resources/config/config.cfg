# Timeout setting


# This is a sleep after get(URL), NOT the WebDriver pageloadtimeout

# This is the WebDriver pageloadtimeout

# wait and interval time to class creation in seconds for each env


#browser=chrome
chrome.driver.path = ./src/test/resources/drivers/chromedriver.exe
#browser=ie
ie.driver.path = ./src/test/resources/drivers/iedriver.exe

# URLs


# Environment
environment=stg
browser=firefox
